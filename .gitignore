# ── Python ──────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg
*.egg-info/
dist/
build/
.eggs/
node_modules/
dist-desktop/
src-tauri/target/
src-tauri/gen/
src-tauri/binaries/ledger-engine-*.exe
src-tauri/icons/android/
src-tauri/icons/ios/
src-tauri/icons/*.icns
src-tauri/icons/Square*Logo.png
src-tauri/icons/StoreLogo.png
*.tsbuildinfo

# ── Virtual environments ─────────────────────────────────────────────────
.venv/
.venv*/
venv/
env/
ENV/

# ── Database (contains personal financial data — do NOT commit) ──────────
data/finance.db
data/watcher_state.json
data/*.db
data/*.db-shm
data/*.db-wal
data/*.json
data/backups/
data/.*.tmp*

# Demo data should be generated locally and reviewed before sharing.

# ── Launcher / runtime artifacts ─────────────────────────────────────────
launcher.log
launcher.log.prev
launcher.log.*
exports/

# ── Bug-report bundles (sanitized but local-only) ───────────────────────
exports/bug_reports/

# ── Streamlit secrets ────────────────────────────────────────────────────
.streamlit/secrets.toml

# ── AI categorization config (contains API key — never commit, never export) ─
config.json

# ── Environment files ────────────────────────────────────────────────────
.env
.env.*
!.env.example

# ── OS files ─────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Editor ───────────────────────────────────────────────────────────────
.idea/
.vscode/
.claude/
.codegraph/
*.swp
*.swo

# ── Temp / test files ─────────────────────────────────────────────────────
*.tmp
*.bak
ledger_test_*.pdf

# ── Statement / import artifacts (use demo data in public examples) ───────
*.pdf
*.ofx
*.qfx
*.qif
*.ofc
Tangerine Statements/

# Internal handoff / local working notes
CLAUDE_HANDOFF.md

# local Claude pointer (committed guide is AGENTS.md)
CLAUDE.md

# Release artifact: built per release and uploaded to GitHub, never committed.
# The signature inside it is only meaningful next to the build that produced it.
latest.json
